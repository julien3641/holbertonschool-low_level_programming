#include "main.h"
#include <string.h>

/**
 * _strlen - longeur d'un champs
 * @s: valeur de a
 *
 * Return: Return 0
 */
int _strlen(char *s)
{
	int nbcar;
	{
		nbcar = strlen(s);
			}
	return (nbcar);
}
